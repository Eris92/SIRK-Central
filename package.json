{
  "name": "sirk-central",
  "version": "0.2.0",
  "private": true,
  "description": "Central management plane and outbound tunnel broker for SIRK Portal.",
  "main": "src/entry.js",
  "scripts": {
    "start": "node -r ./src/preload-hardening.js src/entry.js",
    "dev": "node --watch -r ./src/preload-hardening.js src/entry.js",
    "test": "node --test",
    "hash-password": "node scripts/hash-password.js",
    "generate-access-key": "node scripts/generate-access-key.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "8.21.1"
  }
}
