{
  "name": "sirk-central",
  "version": "1.0.0-rc.28",
  "private": true,
  "description": "SIRK Central production-ready management plane.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "pretest": "node scripts/validate-runtime-architecture.js",
    "test": "node scripts/run-node-tests.js",
    "check:syntax": "find deploy scripts -maxdepth 1 -type f -name '*.sh' -print0 | xargs -0 -n1 bash -n && find src updater scripts public test -type f -name '*.js' -print0 | xargs -0 -n1 node --check && python3 -m py_compile scripts/validate-backup-archive.py && node scripts/validate-runtime-architecture.js",
    "check:architecture": "node scripts/validate-runtime-architecture.js",
    "check": "npm run check:syntax && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "ws": "8.21.1"
  }
}
